apiVersion: v1
kind: Service
metadata:
  name: processing
  namespace: hackaton
  labels:
    app: processing
spec:
  selector:
    app: processing
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
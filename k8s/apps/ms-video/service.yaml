apiVersion: v1
kind: Service
metadata:
  name: video-service
  namespace: hackaton
  labels:
    app: video-service
spec:
  selector:
    app: video-service
  ports:
    - name: http
      port: 80
      targetPort: 8081
  type: ClusterIP